def first_palindromic_string(words):
    for word in words:
        if word == word[::-1]:
            return word
    return " "
words = ["abc", "car", "racecar", "cool"]
print(first_oalindromic_string(words))
OUTPUT:
"racecar"
